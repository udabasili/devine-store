import { device } from '@/utils/responsiveBreakpoints'
import styled from 'styled-components'

export const AuthFormContainer = styled.div`
    border: 2px solid #eaeaea;
    padding: 4rem;
    height: max-content;
    box-shadow: -2px 10px 23px 2px rgba(0,0,0,0.28);
`